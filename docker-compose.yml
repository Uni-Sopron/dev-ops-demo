services:
  website:
    image: nginx:alpine
    ports:
      - 8082:80
    networks:
      - nginx-proxy-network
    restart: unless-stopped
    volumes:
      - $WEBSITE_PATH:/usr/share/nginx/html

networks:
  nginx-proxy-network:
    external: true
